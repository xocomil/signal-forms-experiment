import { HeaderDirectionPipe } from './headerDirection-pipe';

describe('HeaderDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
